# Local Run Guide

1. Start the app with:
   `python server.py`
2. Open:
   `http://127.0.0.1:8000`

Admissions submissions are stored in a local SQLite database file.

Default Windows location:
`%LOCALAPPDATA%\GreenValleyPublicSchool\school.db`
